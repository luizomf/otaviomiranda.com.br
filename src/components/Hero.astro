---
/**
 * ARQUIVO: Hero.astro
 *
 * O QUE FAZ:
 *   Secao hero da landing page com apresentacao pessoal, animacao de
 *   typewriter no titulo (CSS puro via @keyframes), links em formato
 *   "pill" (PillLink) e o canvas de particulas (HeroCanvas) como fundo.
 *   Ocupa 100% da viewport (min-height: 100svh).
 *
 * USADO EM:
 *   - pages/index.astro (landing page — unico local de uso)
 *
 * CONCEITO ASTRO:
 *   - Composicao de componentes: Hero importa e usa HeroCanvas e PillLink
 *     como tags. Cada componente e independente e isolado, mas juntos
 *     formam a secao hero completa. Isso e o modelo de composicao do Astro.
 *   - Animacoes CSS puras: typewritter e blinking-caret sao @keyframes
 *     definidos no <style> com escopo. Nenhum JS e necessario para isso.
 *   - <style> com escopo (scoped): todas as classes (.section-hero,
 *     .hero-content, .hero-title, etc.) so existem neste componente.
 *     O Astro adiciona data-astro-cid-xxx para isolar o CSS.
 *   - Sem <slot />: este componente nao aceita conteudo filho — a
 *     estrutura do hero e fixa e auto-contida.
 */
import HeroCanvas from './HeroCanvas.astro';
import PillLink from './PillLink.astro';
---

<section id='section-hero' class='section section-dark section-hero'>
  <div class='section-content hero-content'>
    <header>
      <p class='hero-eyeball'>Olá, sou</p>
      <h1 class='hero-title'>Otávio Miranda</h1>
      <p class='hero-line'>
        Desenvolvedor de software e criador de conteúdo desde 2009.
      </p>
      <p class='hero-line'>
        Educador em cursos de programação com mais de 300 mil alunos.
      </p>
      <p class='hero-line'>Fique à vontade para explorar.</p>
    </header>

    <div class='hero-links'>
      <PillLink href='#courses'>Cursos</PillLink>

      <PillLink
        target='_blank'
        rel='noreferrer noopener'
        href='https://www.youtube.com/@otaviomiranda/videos'>YouTube</PillLink
      >

      <PillLink href='/contacts/'>Contatos</PillLink>
    </div>
  </div>

  <div class='next-section'>
    <a href='#courses'> Continue ▼ </a>
  </div>

  <HeroCanvas />
</section>

<style>
  .section-hero {
    min-height: 100svh;
    display: grid;
    place-items: center;
    font-family: var(--font-family-code);
    position: relative;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    text-align: center;
    justify-content: center;
    max-width: 75ch;
  }

  .hero-eyeball {
    margin: 0.5rem 0;
    line-height: var(--line-height-100);
    font-size: var(--font-size-text-400);
    color: var(--color-text-subtle-on-dark);
  }

  .hero-title {
    margin: 0 auto;
    margin-top: 1rem;
    margin-bottom: 2rem;
    font-size: var(--font-size-display-fluid);
    white-space: nowrap;
    border-right: 1rem solid currentcolor;
    overflow: hidden;
    animation:
      typewritter 20s steps(20, end) alternate infinite,
      blinking-caret 0.75s step-end infinite;
  }

  .hero-line {
    margin: 1.5rem 0;
    line-height: 1.5;
    font-size: var(--font-size-text-500);
    color: var(--color-text-subtle-on-dark);
  }

  .hero-links {
    margin: clamp(var(--space-900), 4vw, var(--space-1500)) auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: clamp(var(--space-600), 3vw, var(--space-900))
      clamp(var(--space-500), 3vw, var(--space-700));
    max-width: 80%;

    a {
      text-decoration: none;
    }
  }

  @keyframes typewritter {
    0% {
      width: 0;
    }

    7% {
      width: 100%;
    }
  }

  @keyframes blinking-caret {
    from,
    to {
      border-color: transparent;
    }

    50% {
      border-color: currentcolor;
    }
  }
</style>
