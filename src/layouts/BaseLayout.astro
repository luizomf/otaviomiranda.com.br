---
/**
 * ARQUIVO: BaseLayout.astro
 *
 * O QUE FAZ:
 *   Layout principal do site. Monta a estrutura HTML completa (<html>, <head>,
 *   <body>) com todas as meta tags de SEO (Open Graph, Twitter Card, canonical,
 *   robots) e importa o CSS global. O conteudo de cada pagina e injetado pelo
 *   componente <slot />, que funciona como um "buraco" onde o conteudo filho
 *   sera inserido.
 *
 * USADO EM:
 *   - src/pages/index.astro (pagina inicial)
 *   - src/pages/[...slug].astro (posts individuais do blog)
 *   - src/pages/blog/[page].astro (arquivo paginado do blog)
 *   - src/pages/contacts.astro (pagina de contatos)
 *
 * CONCEITO ASTRO:
 *   Layouts - arquivos em src/layouts/ que definem a estrutura HTML reutilizavel.
 *   O <slot /> e o mecanismo do Astro (inspirado nos Web Components) que permite
 *   ao layout receber conteudo dinamico dos componentes filhos. As props sao
 *   recebidas via Astro.props, que e a forma do Astro acessar os atributos
 *   passados ao componente (ex: <BaseLayout title="..." description="...">).
 *   Tudo dentro das cercas (---) e o "frontmatter" do componente, executado
 *   apenas no servidor durante o build (SSG).
 */
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonical?: string;
  robots?: string;
  ogType?: string;
}

const {
  title = 'Otávio Miranda | Cursos de Programação Python, JS e Full Stack',
  description = 'Aprenda programação de verdade com Otávio Miranda. Cursos de Python, JavaScript, TypeScript e Full Stack focados em projetos reais. Mais de 300 mil alunos.',
  image = 'https://otaviomiranda.com.br/imgs/curso-javascript-typescript.webp',
  canonical,
  robots = 'index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1',
  ogType = 'website',
} = Astro.props;

const site = Astro.site ?? new URL('https://www.otaviomiranda.com.br');
const canonicalUrl = canonical ?? new URL(Astro.url.pathname, site).toString();
---

<!doctype html>
<html lang='pt-BR'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <link rel='sitemap' href='/sitemap-index.xml' />
    <link rel='canonical' href={canonicalUrl} />
    <meta name='robots' content={robots} />

    <title>{title}</title>
    <meta name='description' content={description} />

    <!-- Open Graph / Sociabilidade -->
    <meta property='og:title' content={title} />
    <meta property='og:description' content={description} />
    <meta property='og:type' content={ogType} />
    <meta property='og:url' content={canonicalUrl} />
    <meta property='og:site_name' content='Otávio Miranda' />
    <meta property='og:locale' content='pt_BR' />
    <meta property='og:image' content={image} />

    <!-- Twitter -->
    <meta name='twitter:card' content='summary_large_image' />
    <meta name='twitter:title' content={title} />
    <meta name='twitter:description' content={description} />
    <meta name='twitter:image' content={image} />

    <!-- Favicon mapped to /public -->
    <link rel='icon' type='image/webp' href='/imgs/favicon-1.webp' />
  </head>
  <body>
    <main class='main'>
      <slot />
    </main>
  </body>
</html>
